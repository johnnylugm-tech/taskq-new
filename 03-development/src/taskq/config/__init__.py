"""taskq.config — runtime configuration.

[FR-06] Holds the ``Settings`` value object that other layers consult
for connection-pool sizing and pre-ping policy. Citations:
SPEC.md §3 FR-06 (pool_size + pool_pre_ping), §5.1.
"""
